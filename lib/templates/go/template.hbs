package {{package}}

{{#each comments}}
// {{this}}
{{/each}}
type {{name}} struct {
{{#each attributes}}
	{{#each comments}}
	// {{this}}
	{{/each}}
	{{name}}: {{type}} {{json}}
{{/each}}
}

func New{{name}} ({{params}}) *{{name}} {
	return &{{name}} {
		{{body}}
	}
}